# build output
dist/
.astro/

# dependencies
node_modules/

# app runtime data — SQLite database + uploaded images (persisted in a volume)
data/

# legacy pocketbase runtime data (kept for reference, never committed)
pocketbase/pb_data/
pocketbase/pocketbase

# logs / env
*.log
.env
.env.local
.env.production

# os / editor
.DS_Store
.idea/
.vscode/
