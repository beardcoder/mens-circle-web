---
import { Icon } from 'astro-icon/components';

interface Props {
  class?: string;
}

const { class: className = 'logo__icon' } = Astro.props;
---

<Icon name="logo" class={className} aria-hidden="true" />
