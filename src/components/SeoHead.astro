---
import site from '../data/site.json';
import { canonicalUrl } from '../lib/canonical';

// Font preloading is handled by <Font preload /> in the layout head.

interface Props {
  title?: string;
  description?: string;
  ogType?: string;
  /** Open Graph / social title. Falls back to `title` (then the site name). */
  ogTitle?: string;
  /** Open Graph / social description. Falls back to `description`. */
  ogDescription?: string;
  ogImage?: string;
  ogImageAlt?: string;
  /** Pixel size of `ogImage`. Both or neither, and only when actually known. */
  ogImageWidth?: number;
  ogImageHeight?: number;
  /** Up to two labelled facts for `twitter:label1/data1` and `label2/data2`. */
  socialDetails?: { label: string; value: string }[];
  robots?: string;
  /** Extra JSON-LD objects to embed (Article, FAQPage, Event, …). */
  schemas?: Record<string, unknown>[];
  /** ISO dates for an `ogType="article"` page. Emitted as `article:*` meta. */
  publishedTime?: string;
  modifiedTime?: string;
}

const {
  title,
  description = site.description,
  ogType = 'website',
  ogTitle,
  ogDescription,
  ogImage,
  ogImageAlt = `${site.siteName} – ${site.tagline}`,
  ogImageWidth,
  ogImageHeight,
  socialDetails = [],
  // `index, follow` alone leaves Google's preview limits at their defaults. The
  // max-* directives lift them, which is what allows a large image in Search.
  robots = 'index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1',
  schemas = [],
  publishedTime,
  modifiedTime,
} = Astro.props;

const siteUrl = Astro.site ?? new URL('https://mens-circle.de');

// One spelling per page, shared with the pages' own structured data.
const canonical = canonicalUrl(siteUrl, Astro.url.pathname);

// Umami, self-hosted — the only third-party origin the site loads. Production
// builds only, so local sessions never count as visits.
const umamiOrigin = new URL(import.meta.env.PUBLIC_UMAMI_ENDPOINT || 'https://va.letsbenow.de').origin;
const umamiWebsiteId = import.meta.env.PUBLIC_UMAMI_ID || 'f2964cb9-28e6-4658-810f-2acfb9cb9c46';
const fullTitle = title ?? `${site.siteName}${site.tagline ? ` – ${site.tagline}` : ''}`;
const socialTitle = ogTitle ?? title ?? site.siteName;
const socialDescription = ogDescription ?? description;
// Declared dimensions must match the file, or a summary_large_image card gets
// downgraded to a small one. So width/height go out only when the caller knows
// them; otherwise the scraper measures the file itself.
const usesDefaultImage = !ogImage;
const ogImageUrl = new URL(ogImage ?? '/images/og-default.png', siteUrl).href;
const imageWidth = usesDefaultImage ? 1200 : ogImageWidth;
const imageHeight = usesDefaultImage ? 630 : ogImageHeight;

const MIME_BY_EXTENSION: Record<string, string> = {
  png: 'image/png',
  jpg: 'image/jpeg',
  jpeg: 'image/jpeg',
  webp: 'image/webp',
  gif: 'image/gif',
  avif: 'image/avif',
};
// From the path, not the href: a `?v=` cache token would read as the extension.
const imageType = MIME_BY_EXTENSION[new URL(ogImageUrl).pathname.split('.').pop()?.toLowerCase() ?? ''];

const cardDetails = socialDetails.slice(0, 2);

const sameAs = site.socialLinks.filter((l) => !['email', 'phone', 'whatsapp'].includes(l.type)).map((l) => l.value);

// One `@id` for Markus, referenced from the business below and from the Article
// on his page, so the three mentions tie together.
const person = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  '@id': `${siteUrl.origin}/#markus`,
  name: 'Markus Sommer',
  url: new URL('/warum-ich-den-maennerkreis-leite', siteUrl).href,
  image: new URL('/images/og-markus.jpg', siteUrl).href,
  worksFor: { '@id': `${siteUrl.origin}/#organization` },
};

const localBusiness = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  '@id': `${siteUrl.origin}/#organization`,
  name: site.siteName,
  description: site.description,
  url: siteUrl.origin,
  // An ImageObject, not a bare URL: Google prefers explicit logo dimensions.
  logo: {
    '@type': 'ImageObject',
    url: new URL('/images/logo-color.png', siteUrl).href,
    width: 512,
    height: 513,
  },
  image: new URL('/images/og-default.png', siteUrl).href,
  email: site.contactEmail,
  telephone: site.contactPhone,
  founder: { '@id': `${siteUrl.origin}/#markus` },
  address: {
    '@type': 'PostalAddress',
    addressLocality: site.geo.locality,
    addressRegion: site.geo.region,
    postalCode: site.geo.postalCode,
    addressCountry: site.geo.country,
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: site.geo.lat,
    longitude: site.geo.lng,
  },
  areaServed: {
    '@type': 'GeoCircle',
    geoMidpoint: {
      '@type': 'GeoCoordinates',
      latitude: site.geo.lat,
      longitude: site.geo.lng,
    },
    geoRadius: 50000,
  },
  priceRange: '€',
  ...(sameAs.length ? { sameAs } : {}),
};

const website = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  '@id': `${siteUrl.origin}/#website`,
  name: site.siteName,
  url: siteUrl.origin,
  description: site.description,
  inLanguage: 'de-DE',
  publisher: { '@id': `${siteUrl.origin}/#organization` },
};

const allSchemas = [localBusiness, person, website, ...schemas];
---

<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

<title>{fullTitle}</title>
<meta name="title" content={fullTitle} />
<meta name="description" content={description} />
<meta name="author" content="Markus Sommer" />
<link rel="canonical" href={canonical} />

<link rel="alternate" hreflang="de" href={canonical} />
<link rel="alternate" hreflang="x-default" href={canonical} />

<meta name="theme-color" content={site.themeColor} />
<meta name="color-scheme" content="light dark" />
<meta name="msapplication-TileColor" content={site.themeColor} />

{import.meta.env.PROD && (
  <>
    <link rel="preconnect" href={umamiOrigin} crossorigin />
    <script is:inline defer src={`${umamiOrigin}/script.js`} data-website-id={umamiWebsiteId} data-performance="true" />
  </>
)}

<!-- Open Graph -->
<meta property="og:type" content={ogType} />
<meta property="og:url" content={canonical} />
<meta property="og:title" content={socialTitle} />
<meta property="og:description" content={socialDescription} />
<meta property="og:image" content={ogImageUrl} />
{/* Legacy, and Facebook still reads it: some of its pipeline prefers the
    explicitly-secure URL over og:image when the two differ. Ours never do. */}
<meta property="og:image:secure_url" content={ogImageUrl} />
{imageType && <meta property="og:image:type" content={imageType} />}
{imageWidth && imageHeight && <meta property="og:image:width" content={String(imageWidth)} />}
{imageWidth && imageHeight && <meta property="og:image:height" content={String(imageHeight)} />}
<meta property="og:image:alt" content={ogImageAlt} />
<meta property="og:locale" content="de_DE" />
<meta property="og:site_name" content={site.siteName} />
{ogType === 'article' && publishedTime && <meta property="article:published_time" content={publishedTime} />}
{ogType === 'article' && modifiedTime && <meta property="article:modified_time" content={modifiedTime} />}
{ogType === 'article' && <meta property="article:author" content="Markus Sommer" />}

<!-- Twitter -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:url" content={canonical} />
<meta name="twitter:title" content={socialTitle} />
<meta name="twitter:description" content={socialDescription} />
<meta name="twitter:image" content={ogImageUrl} />
<meta name="twitter:image:alt" content={ogImageAlt} />
{cardDetails.map((detail, index) => (
  <>
    <meta name={`twitter:label${index + 1}`} content={detail.label} />
    <meta name={`twitter:data${index + 1}`} content={detail.value} />
  </>
))}

<!-- Robots -->
<meta name="robots" content={robots} />
<meta name="googlebot" content={robots} />

<!-- Favicon -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
<link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
<link rel="apple-touch-icon" sizes="180x180" href="/favicon-180x180.png" />
<link rel="manifest" href="/manifest.webmanifest" />

<!-- Structured data -->
{allSchemas.map((schema) => (
  <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
))}
