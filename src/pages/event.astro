---
import Layout from '../layouts/Layout.astro';
import { fetchNextEvent } from '@lib/pocketbase-server';
import EventPage from '../components/islands/EventPage.svelte';
import site from '../data/site.json';

// SSR: the next upcoming event (incl. live capacity) is fetched from PocketBase
// per request, so /event always reflects the current "next" event with no
// rebuild. `null` renders the graceful "no termin scheduled" state.
const event = await fetchNextEvent();
---

<Layout
  title="Termin & Anmeldung – Männerkreis Niederbayern/ Straubing"
  description="Der nächste Männerkreis in Straubing. Sieh dir den Termin an und sichere dir deinen Platz."
  ogType="event"
>
  <EventPage
    client:load
    event={event}
    whatsappLink={site.whatsappCommunityLink}
  />
</Layout>

<style is:global>
  @layer sections {
    /* ---- Event Register Section ---- */
    .event-register-section {
      --_surface-noise-opacity: 0.03;

      position: relative;
      background: var(--bg-dark);
      overflow: hidden;
      color: var(--text-on-dark);
    }

    .event-register__layout {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-block-size: 100svh;
    }

    @media (width <= 900px) {
      .event-register__layout {
        grid-template-columns: 1fr;
        min-block-size: auto;
      }
    }

    .event-register__content {
      display: flex;
      position: relative;
      flex-direction: column;
      justify-content: center;
      padding: var(--space-xl) var(--space-lg);
      overflow: hidden;
    }

    @media (width <= 900px) {
      .event-register__content {
        padding: var(--space-xl) var(--space-md);
        text-align: center;
      }
    }

    .event-register__circles {
      opacity: 0.15;
    }

    .event-register__circle--1 {
      animation: breathe 20s var(--ease-ambient) infinite;
      inset-block-start: -60%;
      inset-inline-start: -30%;
      inline-size: 120%;
      block-size: 120%;
    }

    .event-register__circle--2 {
      animation: breathe-slow 26s var(--ease-ambient) infinite -7s;
      inset-block-end: -40%;
      inset-inline-end: -20%;
      inline-size: 80%;
      block-size: 80%;
    }

    .event-register__eyebrow {
      position: relative;
      z-index: 1;
    }

    .event-register__title {
      position: relative;
      z-index: 1;
      margin-block-end: var(--space-lg);
      color: var(--text-on-dark);
    }

    .event-register__spots {
      display: flex;
      position: relative;
      align-items: baseline;
      gap: var(--space-xs);
      z-index: 1;
      color: var(--color-sand);
      font-size: var(--text-lg);
    }

    @media (width <= 900px) {
      .event-register__spots {
        justify-content: center;
      }
    }

    .event-register__spots-available {
      color: var(--accent-secondary);
      font-weight: 700;
      font-size: var(--text-2xl);
      line-height: 1;
      font-family: var(--font-display);
    }

    .event-register__spots-full {
      color: var(--accent-primary);
      font-weight: 700;
      font-size: var(--text-xl);
      font-family: var(--font-display);
    }

    .event-register__spots-hint {
      position: relative;
      z-index: 1;
      margin-block-start: var(--space-sm);
      max-inline-size: 32ch;
      color: color-mix(in oklch, var(--color-sand) 75%, transparent);
      font-size: var(--text-body-compact);
      line-height: 1.5;
    }

    .event-register__form-wrap {
      display: flex;
      position: relative;
      justify-content: center;
      align-items: center;
      border-inline-start: 1px solid var(--border-on-dark);
      background: color-mix(in oklch, var(--bg-primary) 5%, transparent);
      padding: var(--space-xl) var(--space-lg);
    }

    @media (width <= 900px) {
      .event-register__form-wrap {
        border-inline-start: none;
        border-block-start: 1px solid var(--border-on-dark);
        padding: var(--space-xl) var(--space-md);
      }
    }

    .event-register__form {
      inline-size: 100%;
      max-inline-size: var(--content-max-form);
    }

    .event-register__submit {
      margin-block-start: var(--space-md);
      inline-size: 100%;
    }

    .event-register__submit:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .event-register__past-info {
      inline-size: 100%;
      max-inline-size: var(--content-max-form);
      text-align: center;
    }

    .event-register__past-text {
      margin-block-end: var(--space-md);
      color: var(--text-secondary);
      font-size: var(--text-lg);
      line-height: 1.6;
    }

    .event-register__past-text:last-of-type {
      margin-block-end: var(--space-lg);
    }

    @media (width <= 900px) {
      .event-register__past-info {
        text-align: start;
      }
    }

    /* ---- Event Info Section ---- */
    .event-info-section {
      position: relative;
      background: var(--bg-primary);
      padding: var(--space-2xl) 0;
      overflow: hidden;
    }

    .event-info__bg-text {
      position: absolute;
      transform: translateY(-50%);
      opacity: 0.25;
      inset-block-start: 50%;
      inset-inline-end: var(--space-md);
      pointer-events: none;
      color: var(--border-light);
      font-weight: 800;
      font-size: clamp(6rem, 18vw, 16rem);
      line-height: 0.8;
      font-family: var(--font-display);
      letter-spacing: 0.02em;
      white-space: nowrap;
    }

    @media (width <= 800px) {
      .event-info__bg-text {
        display: none;
      }
    }

    .event-info__grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
      border-block-start: 1px solid var(--border-light);
      border-block-end: 1px solid var(--border-light);
    }

    @media (width <= 1000px) {
      .event-info__grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (width <= 600px) {
      .event-info__grid {
        grid-template-columns: 1fr;
      }
    }

    .event-info__card {
      position: relative;
      border-inline-end: 1px solid var(--border-light);
      padding: var(--space-lg);
      overflow: hidden;
    }

    .event-info__card:last-child {
      border-inline-end: none;
    }

    @media (width <= 1000px) {
      .event-info__card:nth-child(2n) {
        border-inline-end: none;
      }

      .event-info__card:nth-child(1),
      .event-info__card:nth-child(2) {
        border-block-end: 1px solid var(--border-light);
      }
    }

    @media (width <= 600px) {
      .event-info__card {
        border-inline-end: none;
        border-block-end: 1px solid var(--border-light);
      }

      .event-info__card:last-child {
        border-block-end: none;
      }
    }

    .event-info__card-circle {
      position: absolute;
      opacity: 0.3;
      inset-block-start: -30%;
      inset-inline-end: -30%;
      border: 1px solid var(--border-light);
      border-radius: 50%;
      aspect-ratio: 1 / 1;
      inline-size: 60%;
    }

    .event-info__card h3 {
      margin-block-end: var(--space-xs);
      color: var(--text-secondary);
      font-weight: 600;
      font-size: var(--text-xs);
      letter-spacing: 0.2em;
      text-transform: uppercase;
    }

    .event-info__card-value {
      margin-block-end: 0.25rem;
      color: var(--text-primary);
      font-weight: 600;
      font-size: clamp(1.5rem, 3vw, 2.25rem);
      line-height: 1.1;
      font-family: var(--font-display);
    }

    .event-info__card-sub {
      color: var(--text-secondary);
      font-size: var(--text-body-compact);
    }

    .event-info__calendar {
      margin-block-start: var(--space-xl);
      text-align: center;
    }

    /* ---- Event About Section ---- */
    .event-about-section {
      background: var(--bg-primary);
      overflow: hidden;
    }

    .event-about__layout {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      min-block-size: 80vh;
    }

    @media (width <= 1000px) {
      .event-about__layout {
        grid-template-columns: 1fr;
        min-block-size: auto;
      }
    }

    .event-about__content {
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: var(--space-xl) var(--space-lg);
      padding-inline-end: var(--space-xl);
    }

    @media (width <= 1000px) {
      .event-about__content {
        order: 2;
        padding: var(--space-xl) var(--space-md);
      }
    }

    .event-about__title {
      max-inline-size: var(--content-max-wide);
    }

    .event-about__text {
      max-inline-size: var(--content-max-text);
      color: var(--text-secondary);
      font-size: var(--text-section-body-lg);
      line-height: var(--leading-relaxed);
    }

    .event-about__text p {
      margin-block-end: 1em;
    }

    .event-about__visual {
      display: flex;
      position: relative;
      justify-content: center;
      align-items: center;
      background: var(--bg-section-alt);
      overflow: hidden;
    }

    @media (width <= 1000px) {
      .event-about__visual {
        order: 1;
        min-block-size: 50vh;
      }
    }

    .event-about__quote-area {
      --_surface-noise-opacity: 0.05;
      display: flex;

      position: absolute;
      justify-content: center;
      align-items: center;
      inset: 0;
      overflow: hidden;
    }

    .event-about__circles {
      opacity: 0.2;
    }

    .event-about__circle--1 {
      animation: breathe 20s var(--ease-ambient) infinite;
      inset-block-start: -20%;
      inset-inline-start: -20%;
      inline-size: 140%;
      block-size: 140%;
    }

    .event-about__circle--2 {
      transform: translate(-50%, -50%);
      animation: breathe-slow 28s var(--ease-ambient) infinite -5s;
      inset-block-start: 50%;
      inset-inline-start: 50%;
      aspect-ratio: 1 / 1;
      inline-size: 80%;
    }

    .event-about__circle--3 {
      animation: breathe 24s var(--ease-ambient) infinite -10s;
      inset-block-end: -10%;
      inset-inline-end: -10%;
      aspect-ratio: 1 / 1;
      inline-size: 50%;
    }

    .event-about__quote {
      position: relative;
      z-index: 1;
      margin-block-end: 0;
      max-inline-size: 500px;
      color: var(--text-on-dark);
      font-style: italic;
      font-size: clamp(2rem, 4vw, 3.5rem);
      line-height: 1.3;
      font-family: var(--font-display);
      text-align: center;
    }

    .event-about__quote .text-italic {
      color: var(--accent-secondary);
    }

    /* ---- Event CTA Section ---- */
    .event-cta-section {
      --_surface-noise-opacity: 0.03;

      position: relative;
      background: var(--bg-dark);
      padding: var(--space-2xl) 0;
      overflow: hidden;
      color: var(--text-on-dark);
      text-align: center;
    }

    .event-cta__circles {
      opacity: 0.15;
    }

    .event-cta__circle--1 {
      transform: translate(-50%, -50%);
      animation: breathe 22s var(--ease-ambient) infinite;
      inset-block-start: 50%;
      inset-inline-start: 50%;
      inline-size: 70vw;
      block-size: 70vw;
    }

    .event-cta__circle--2 {
      transform: translate(-50%, -50%);
      animation: breathe-slow 30s var(--ease-ambient) infinite -5s;
      inset-block-start: 50%;
      inset-inline-start: 50%;
      inline-size: 50vw;
      block-size: 50vw;
    }

    .event-cta__content {
      display: flex;
      position: relative;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 1;
    }

    .event-cta__title {
      margin-block-end: var(--space-lg);
      color: var(--text-on-dark);
    }

    .event-register__form .form-label-optional,
    .event-register__form .form-helper,
    .event-register__form .form-checkbox span {
      opacity: 0.7;
      color: var(--color-sand);
    }

    /* ---- No Event Page ---- */
    .no-event-hero {
      display: flex;
      align-items: center;
      background: var(--bg-section-alt);
      min-block-size: 80vh;
    }

    .no-event-info-section {
      position: relative;
      background: var(--bg-primary);
      overflow: hidden;
    }

    .no-event-info__layout {
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      align-items: center;
      gap: var(--space-xl);
      min-block-size: 60vh;
    }

    @media (width <= 900px) {
      .no-event-info__layout {
        grid-template-columns: 1fr;
        gap: var(--space-lg);
        min-block-size: auto;
      }
    }

    .no-event-info__content {
      padding: var(--space-lg) 0;
    }

    @media (width <= 900px) {
      .no-event-info__content {
        order: 2;
        text-align: center;
      }
    }

    .no-event-info__title {
      max-inline-size: var(--content-max-description);
    }

    @media (width <= 900px) {
      .no-event-info__title {
        max-inline-size: 100%;
      }
    }

    .no-event-info__text {
      max-inline-size: var(--content-max-text);
      color: var(--text-secondary);
      font-size: var(--text-section-body);
      line-height: var(--leading-relaxed);
    }

    @media (width <= 900px) {
      .no-event-info__text {
        max-inline-size: 100%;
      }
    }

    .no-event-info__visual {
      display: flex;
      position: relative;
      justify-content: center;
      align-items: center;
      border-radius: var(--radius-lg);
      background: var(--bg-section-alt);
      min-block-size: 400px;
      overflow: hidden;
    }

    @media (width <= 900px) {
      .no-event-info__visual {
        order: 1;
        min-block-size: 300px;
      }
    }

    .no-event-info__quote-area {
      --_surface-noise-opacity: 0.05;
      display: flex;

      position: absolute;
      justify-content: center;
      align-items: center;
      inset: 0;
      overflow: hidden;
    }

    .no-event-cta-section {
      position: relative;
      background: var(--bg-primary);
      padding: var(--space-2xl) 0;
      text-align: center;
    }

    .no-event-cta__content {
      margin-inline: auto;
      max-inline-size: var(--content-max-description);
    }

    .no-event-cta__text {
      margin-block-end: var(--space-lg);
      color: var(--text-secondary);
      font-size: var(--text-section-body);
      line-height: var(--leading-relaxed);
    }
  }
</style>
